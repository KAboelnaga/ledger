spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/ledger
    username: ledger_app
    password: ledger_pass_123
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    clean-disabled: true